[
  {
  "date": "2026-03-19",
  "lines": ["1. 缓存穿透（Cache Penetration）",
"问题：查询的数据既不在 Redis 中，也不在数据库中。每次请求都会穿透缓存直达数据库，缓存形同虚设。如果攻击者恶意构造大量不存在的 key 进行高并发请求，数据库压力会直线上升甚至宕机。
解决方案：

缓存空值：当数据库也查不到时，将该 key 对应的空值（null）写入 Redis 并设置较短的过期时间。后续相同请求会命中缓存直接返回空值。缺点是如果攻击者使用大量不同的随机 key，会导致 Redis 中存储大量无意义的空值，浪费内存。
布隆过滤器（Bloom Filter）：在缓存之前加一层布隆过滤器，预先存储所有合法的 key。请求进来先查布隆过滤器，如果判定不存在则直接拒绝，不再查询 Redis 和数据库；如果判定可能存在，才继续查询 Redis。布隆过滤器底层是一个 bit 数组配合 k 个哈希函数，通过将 key 映射到多个位上来判断存在性。由于不同 key 的哈希位可能重叠，存在少量误判（false positive），但说"不存在"则一定不存在。
组合使用：布隆过滤器挡掉绝大部分非法请求，少量漏网的再用缓存空值兜底。"],
  "tag": "#redis"
  },
    {
  "date": "2026-03-18",
  "lines": ["今天学了什么", "第二行内容"],
  "tag": "#template"
  }
]
